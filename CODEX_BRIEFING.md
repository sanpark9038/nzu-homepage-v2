# CODEX BRIEFING — N.Z.U KOREANIZATION & SIMPLIFICATION

> Generated by: Antigravity (El-Rade Park)
> Priority: HIGH
> Scope: UI/UX ONLY (Hangeul Integration & Clean Title)

## Context
CEO Sanpark requested to simplify the main title to "N.Z.U" and Koreanize the entire website interface for better accessibility while maintaining the premium look.

---

## Task 1: Main Hero Section Update (`app/page.tsx`)

**Goal:** Change "NEW ZEALAND UNIVERSITY" to "N.Z.U" and translate all tactical terms.

**Action:**
- Replace `NEW ZEALAND` & `UNIVERSITY` with a single, impactive line: `<h1 className="text-8xl md:text-[12rem] font-black tracking-tighter uppercase italic leading-[0.8] mb-4">N.Z.U</h1>`.
- Translate labels:
    - `SOOP STARCRAFT UNIVERSITY · NZU` -> `숲 스타크래프트 대학 · NZU`
    - `Tiers Breakdown` -> `티어표 상세`
    - `The Roster` -> `전체 로스터`
    - `Total Members` -> `총 단원`
    - `Live Now` -> `실시간 방송`
    - `Active Season` -> `활성 시즌`
    - `Team Rank` -> `팀 랭킹`
    - `Live Streamers` -> `실시간 라이브`
    - `The Elite Gods` -> `최정예 엘리트`
    - `VIEW ALL` -> `전체 보기`
    - `Match Brief` -> `최근 전적 브리핑`
    - `HISTORY` -> `히스토리`
    - `No recent matches recorded.` -> `최근 경기 기록이 없습니다.`
    - `Join the NZU Community` -> `NZU 커뮤니티 합류`
    - `Apply Now` -> `가입 신청`

---

## Task 2: Player List Page (`app/players/page.tsx`)

**Action:**
- `Lineup` -> `선수단 로스터`
- `늪지대 클랜의 정식 로스터 데이터베이스` -> `늪지대 클랜 정식 멤버 아카이브`
- `Registry Status` -> `등록 상태`
- `Active {n} Players` -> `활성 선수 {n}명`
- `Player Search` (Placeholder) -> `선수 검색...`
- `Race Filter` (Labels) -> `전체`, `테란`, `저그`, `프로토스`

---

## Task 3: Navbar & Footer Refinement (`components/Navbar.tsx` & `app/page.tsx`)

**Action:**
- Ensure all navigation items reflect the Korean translations (`홈`, `라이브`, `티어표`, `엔트리`, `선수`).
- Footer credits: Keep "Architected by El-Rade Park" but ensure other text is Korean.

---

## Verification & Constraints
- [ ] No data fetching logic is modified.
- [ ] Ensure "N.Z.U" is centered and dramatically emphasized.
- [ ] Typography remains sharp (font-black, italic) even in Korean.

## Completion
1. Verify Korean translations are accurate and professional.
2. Delete this `CODEX_BRIEFING.md` once the UI is polished.
