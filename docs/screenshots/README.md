# Screenshots

This folder is only for local, ad hoc UI captures.

- Do not treat screenshot PNG files here as durable documentation.
- Recreate them with `scripts/screenshot.js` when needed.
- Keep tracked docs in `docs/` and `docs/archive/`, not in image snapshots.
